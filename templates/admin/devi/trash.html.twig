{% extends 'admin/base.admin.html.twig' %}

{% block title %}Corbeille demande de devis{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Liste de demande de devis</h1>
        <hr>
        {% for message in app.flashes('admin_trash_success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

        {{ include('admin/devi/_tableList.html.twig') }}

    </div>
{% endblock %}
{% block scripts %}
    <script>
        $(document).ready(() => {
            $('#data').DataTable({
                "order": [[1, "desc"]]
            });
        });
    </script>
{% endblock %}